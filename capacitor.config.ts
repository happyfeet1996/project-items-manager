import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project-items-manager',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
